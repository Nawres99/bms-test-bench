# Put the repo root on sys.path so `import bms` works when pytest is run from
# the project root (plain `pytest`, like CI does). Without this pytest only adds
# the tests directory and cannot find the bms package.
[pytest]
pythonpath = .
testpaths = tests
